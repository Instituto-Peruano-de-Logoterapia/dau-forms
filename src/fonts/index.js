import { Inter, Poppins } from 'next/font/google';

export const poppins = Poppins({ style: 'normal', subsets: ['latin'], weight: ['700', '400'] });
export const inter = Inter({ subsets: ["latin"] });